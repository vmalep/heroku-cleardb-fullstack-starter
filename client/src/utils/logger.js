/**
 * logs to the console whatever string is passed as parameter
 * @param {string} textToLog The string to log to the console
 * @returns {undefined}
 */
const logger = (textToLog) => {
	console.log(textToLog);
};

export default logger;